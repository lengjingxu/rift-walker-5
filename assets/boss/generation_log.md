# Boss 立绘生成日志

> 每生成一张追加一行：`| 时间(UTC) | floor_id | 文件路径 | 模型 | 尺寸 | 备注 |`

| 时间(UTC) | floor_id | 文件路径 | 模型 | 尺寸 | 备注 |
|-----------|----------|----------|------|------|------|
| —         | —        | —        | —    | —    | 等待 `GPT_IMAGE_API_KEY` 注入后启动首次生成 |
